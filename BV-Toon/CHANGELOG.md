# 更新记录

## 1.1.7 —— MMD 材质还原度大修

这一版把「看起来像 MMD 原生」这件事往前推了一大步，并且修掉了两个让画面发灰/发花的硬 bug。

### 修掉的两个硬 bug

* **泛光出现绿色重影** —— Blender 4.5/5.x 把 Glare 节点的选项从属性改成了**输入插座**，
  而且枚举变成首字母大写（`Bloom` 不是 `BLOOM`）。旧代码用属性赋值 + 静默 `try/except`，
  一个参数都没设上，节点一直跑默认的 `Streaks`/`Ghosts` —— 画面因此出现彩色重影。
  现在按插座名设置，并且**失败会打印**，不再静默吞掉。
* **宝石/闪片/金属全没了** —— 之前为了治"衣服油腻"，我把球面层强度一刀压到 0.30，
  但 MMD 的球面贴图有两种语义：**加算（mode 2）= 闪片、宝石、金属、目光**，
  这一层就是效果本体；**乘算（mode 1）= 布料上的柔和球面**，压它才是"去油腻"该做的。
  现在按模式分开：加算保持 **1.0**，乘算用预设值（**0.30**）。

### 新增

* **MMD 渐变贴图（toon ramp）驱动明暗** —— 按 MMD 的查表方式：**U = 0.5、V = 受光程度**，
  采样材质自带的 toon 贴图（含共享 `toon01–10`）当暗部遮罩；没有该贴图的材质自动退回自建条带。
* **可调描边粗细** —— 面板「描边粗细」+「应用」按钮，改完即时生效，不用重跑一键卡渲。
  默认 **0.08**（= mmd_tools 原值），想加粗直接填 0.2 / 0.4 / 0.8。
* **材质自带描边参数** —— 读 `edge_color` / `edge_weight`，逐材质给 `mmd_edge.<材质名>` 上色。
* **一键卡渲自动把色彩管理切到「标准」** —— AgX/Filmic 会把卡渲的饱和色洗淡、把明暗压平，
  这也是此前"发灰、发淡、偏平"抱怨的一大半原因。
* **环境色（ambient）接入暗部色调** —— 只有环境色**带颜色信息**（三通道不等）时才生效，
  避免被"统一的灰"污染预设的冷色暗部。
* **独立「Toon」标签页** —— 不再和别的插件挤在同一个侧栏标签下。

### 行为调整

* **一键卡渲 = 卡渲 + MMD 边缘预览**；「设置目影」拆成独立按钮（面板「其他设置」里）。
* **默认取消阴影**（`暗部深浅 = 0`、`分界暖边 = 0`）。想要明暗，把「暗部深浅」调上去即可。
* 卡渲节点组装在自带的 `BVToonData/BVToonShading.blend` 里（**可直接打开手改**），
  缺资产时自动回退到代码现场生成。

### 已知未做

* 材质名特殊规则（`发+` 加算高光、`目光/目影`、`肌` 透红、`スフィア`）—— 计划中
* 副纹理（高光贴图）接进高光通道 —— 计划中

### 工具链（`tools/`）

`build_assets.py`（生成资产）· `render_toon_ab.py`（同机位 A/B + 逐像素对比，透明背景只统计模型像素）·
`probe_mask.py`（遮罩探针）· `probe_toon_math.py` / `probe_toon_axis.py`（渐变图数值与轴向）·
`check_toon_ramp.py` / `check_ambient.py` / `check_oneclick.py`（验收脚本）
---

## 1.1.8 —— 衣服不再发深

* **主因找到**：底色链里有个「反差」阶段是往 **`底色²`（平方后的暗版）** 混 ✗ ——
  0.5² = 0.25、0.25² = 0.0625，深色布料被压成死黑。默认 `反差 0.30 → 0.00`
* `贴图亮度 1.05 → 1.28`；乘算球面（布料那层）`0.6 → 0.3`

## 1.1.9 —— 卡渲接管后清掉原有节点

* 一键卡渲套完后，删除 mmd_tools 原来那套（`Mmd Base Tex` / `Mmd Toon Tex` /
  `mmd_shader` / `mmd_tex_uv` / 原理化 BSDF）
* **坑**：第一版按**显示名**匹配（`MMDShaderDev`）✗ —— 真名是小写的 `mmd_shader` ✗

## 1.2.0 —— 「还原成基本材质」尝试重建原始树

* 还原 = 摘描边 ✓ + 清卡渲节点 ✓ + 让 mmd_tools 重建 ✗
* **实测发现 mmd_tools 5.x 没有"重建"能力** ✗：它的 `convert_materials` 是**迁移**
  （源码里直接 `nodes.get("mmd_shader")`），删掉后就再也回不来（重建 0 个）

## 1.2.1 ~ 1.2.4 —— 改成"收进折叠框 + 静音"

* 原节点不再删除，而是收进一个折叠 Frame 并静音；还原时解静音 + 接回输出
* 修 `NodeFrame` 类型名（不是 `ShaderNodeFrame`）
* 修 `node.parent is frame` **恒为假** ✗ —— Blender 的 RNA 包装对象每次访问都重建，
  节点比较必须用 `==`
* 修重复调用 unarchive（第二次必空 → 跑兜底把基础材质盖到刚复原的树上）

## 1.3.0 —— 按要求：去掉「还原成基本材质」+ 非卡渲节点直接删

* 面板按钮与操作符注册都去掉
* 原节点改为**白名单清理**：只留 `bv_*` 与 Material Output
* ⚠️ 原节点从此**真删**，想回 MMD 原始材质只能**重新导入模型**

## 1.3.1 —— 修 1.3.0 白名单误删（重要）

* 白名单把**渐变查表链的 6 个辅助节点也删了** ✗（它们名字是 Blender 自动生成的：
  `Geometry` / `Math.001` / `Combine XYZ` …）→ **渐变贴图失效** ✗
* 我建的节点全部改名 `bv_toon_geo / bv_toon_turn / bv_toon_angle / bv_toon_split /
  bv_toon_v / bv_toon_uv / bv_toon_grey` ✓ → 渐变恢复（验收 33/33 ✓）

## 1.4.0 —— 清理死代码

* 删除 6 处死代码：`apply_edge_settings`、`drop_original_nodes`、
  `archive_original_nodes`、`unarchive_original_nodes`、`FRAME_NAME`、`ORIGINAL_HINTS`
  以及已注销的 `BVTOON_OT_restore` 类
* 新增体检脚本 `tools/audit_bv_toon.py`（注册/面板/节点白名单/渐变链/9 个操作符/
  幂等性/视图变换，一条命令跑完）

### 1.1.7 → 1.4.0 用户可见的变化一览

| 变化 | 说明 |
|---|---|
| 阴影默认**关闭** | `暗部深浅 0`、`分界暖边 0`；想开就调「暗部深浅」 |
| 一键卡渲 | = 卡渲 + 边缘预览；「设置目影」是独立按钮 |
| 描边 | 用材质自带 `edge_color`；面板「描边粗细」+「应用」可调（默认 0.08，绝对值） |
| 宝石/闪片/金属 | 按 MMD 模式分层：加算 1.0（效果本体）、乘算 0.3（去油腻） |
| 衣服颜色 | 不再被「底色²」压深 |
| 材质树 | 只剩 `bv_*` 节点（干净） |
| 泛光 | 修掉绿色重影（Glare 参数在 4.5/5.x 是插座且枚举首字母大写） |
| 视图变换 | 一键卡渲后自动设为「标准」 |
| 还原成基本材质 | **已移除**（要回原始材质需重新导入模型） |
---

## 1.4.1 —— MMD 使用规范随插件包发布

* 新增 `MMD-NOTICE.md`（使用规范 + 署名格式：`模型：<作者>（<链接>）` / `渲染：BVan`），
  **随插件 zip 一起发布**，别人装完在插件目录里就能看到

## 1.4.2 —— 一键卡渲自动烧录署名

* 一键卡渲时把署名烧进图片（Blender 的 stamp note），默认文字 `渲染：BVan`，面板可改/可关
* 首次一键卡渲后提示一次：「发布作品请署名：模型：<作者>（<链接>）」
* **修**：Blender 5.2 的 `RenderSettings` 没有 `use_stamp_metadata`（改成 `stamp_font_size`）

## 1.4.3 —— 水印精简

* 水印只留 **`BVToon`**、alpha **0.85**（85% 不透明）、去掉黑底框、字号 18
* 场景里存的旧文字（`渲染：BVan`）自动迁移

## 1.4.4 —— 只留署名一项

* Blender 的 stamp 是**一整块**：`use_stamp` 一开，`file / date / render time / timecode /
  scene / frame / camera / labels …` 各自还开着 → 现在**逐个关掉**，只留 `use_stamp_note`
* 验收：`仍开着的 stamp 子项=['use_stamp_note']` ✓

## 1.4.5 —— 水印文字定稿

* 默认水印改为 **`BVToon / @BVan`**；旧值（`BVToon`、`渲染：BVan`）自动迁移
* 面板「其他设置」→ `烧录署名`（开关）+ `署名文字`（文本框）随时可改

### 1.4.x 小结（用户可见）

| 变化 | 说明 |
|---|---|
| 材质树干净 | 非卡渲节点（mmd_tools 那套）在接管后全部清理，只留 `bv_*` 与输出 |
| 渐变贴图 | 修 1.3.0 白名单误删导致的失效，查表链节点统一 `bv_toon_*` 前缀 |
| 「还原成基本材质」 | **已移除**（原节点会被清理，想回原始材质请重新导入模型） |
| **水印** | 一键卡渲自动烧录 `BVToon / @BVan`（85% 不透明、无底框、其它 stamp 标签全关） |
| 使用规范 | `MMD-NOTICE.md` 随包发布（署名格式 + 常见条款 + 常见误区） |
| 体检 | `tools/audit_bv_toon.py` 一条命令跑 20+ 项检查 |
---

## 1.4.6 —— 水印改成真正透明

* 之前把「85% 透明度」理解成了 85% **不**透明（alpha 0.85，看着是实心）✗
  → 现在默认 **alpha 0.15（= 85% 透明）**
* 面板新增 **`不透明度`** 控件，可实时调（0.05 很淡 ~ 1.0 实心）
## 1.4.7 —— 水印透明度改为 95%

* 默认 **alpha 0.05（= 95% 透明）**，非常淡；面板 `不透明度` 可随时调回去（0.15 = 85% 透明 / 0.3 更明显）
## 1.4.8 —— 水印透明度定为 85%

* 默认 **alpha 0.15（= 85% 透明）**（1.4.7 的 0.05 太淡，回退）
* 面板 `不透明度` 控件仍在，可实时微调
## 1.4.9 —— 水印文字改为 `渲染 BVToon / @BVan`

* 默认水印文字：**`渲染 BVToon / @BVan`**（85% 透明、无底框、其它 stamp 标签全关）
* 旧值（`BVToon / @BVan`、`BVToon`、`渲染：BVan`）自动迁移
## 1.5.0 / 1.5.0 —— 强制水印（按下渲染的一瞬间）

* 新增 `render_pre` 处理器：**渲染前一刻**把署名强制设好（`use_stamp` 打开、只留署名一项、
  文字 `渲染 BVToon / @BVan`、85% 透明、无底框）—— 面板上的开关被关掉、文字被改错也绕不过
* 验收（故意破坏后真渲染一次）：
  ```
  FW2 handlers=1
  [BV-Toon] 渲染前已强制署名：渲染 BVToon / @BVan
  FW2 真渲染后: use_stamp=True text='渲染 BVToon / @BVan' fg=(1.0,1.0,1.0,0.15)
  ```
* **两个坑**：① `bpy.app.handlers` 的列表**不支持切片赋值** ✗（改成按名字去重后 append ✓）；
  ② 工厂重置 / 重载脚本会清空处理器列表，而模块被缓存后不再重新执行 ✗ →
  改为**每次画面板时补挂一次**（按名字去重 ✓）
## 1.5.0 —— 发布版说明（版本号合并）

* 1.4.6 ~ 1.5.0 的改动**合并进 1.5.0** 发布（水印文字/透明度几次微调 + 渲染前强制署名），
  插件 `bl_info` 版本与 zip 名统一为 **1.5.0**
## 1.5.0 —— 三语使用许可 + 面板语言切换按钮

* 许可改为**自定义使用许可（中 / 英 / 日 三语）**：
  ① 允许使用（含商用）② **禁止再分发** ③ **禁止发布修改版** ④ **必须标明原作者**
  `渲染 BVToon / @BVan` ⑤ 无担保 ⑥ 违反即终止
* 面板「其他设置」新增 **`使用条款`** 行 + 三个按钮：**中文 / EN / 日本語**，各自弹窗显示对应语言条款
* 插件包内 `LICENSE` 同步为三语全文；README / RELEASE / MMD-NOTICE / PUBLISH 的许可说明已替换

> 说明：这不是开源许可（开源要求允许再分发与修改）。GitHub 会显示 "Unknown license"，
> 属正常现象。

## 文档三语化

README / RELEASE / MMD-NOTICE / PUBLISH 各自提供 中文（.md）/ English（.en.md）/ 日本語（.ja.md）三个版本，顶部带语言切换条；LICENSE 为三语同文件。


## 版本号统一为 1.5.0

1.5.0 与 1.5.1 的内容合并发布，插件 `bl_info`、zip 名、文档与 Release tag 全部统一为 **1.5.0**。
